# 我的笔记本 - Cloudflare Workers

基于 Cloudflare Workers 的笔记本式书签管理系统，支持详细笔记、分页显示和浮动详情面板。

## ✨ 功能特性

- 📝 笔记本式界面，支持标题、链接、描述、标签等详细信息
- 📄 分页显示（每页50条）
- 🔍 点击展开浮动详情面板（类似知识库）
- ✏️ 前端在线编辑（点击右下角 ✏️ 按钮）
- 💾 KV 存储持久化
- 🎨 现代化UI设计

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置 KV 命名空间

```bash
# 自动创建（推荐）
npm run setup-kv

# 或使用已有 ID
npm run setup-kv:id YOUR_GLOBAL_ID
```

### 3. 部署

#### 部署到 Cloudflare Workers

```bash
# 登录 Cloudflare
wrangler login

# 部署到 Cloudflare
npm run deploy
# 或使用脚本
./deploy-cloudflare.sh
```

#### 部署到 GitHub

```bash
# 首次部署需要设置远程仓库
git remote add origin <your-repo-url>

# 部署到 GitHub
./deploy-github.sh
```

## 📖 使用方法

- **查看笔记**：在主页面浏览笔记列表
- **查看详情**：点击笔记项展开右侧浮动详情面板
- **编辑笔记**：点击右下角 ✏️ 按钮
- **分页浏览**：超过50条自动分页

## 📝 项目结构

```
bookmark-notebook/
├── src/
│   ├── index.js      # Workers 主文件
│   ├── api.js        # API处理函数
│   ├── template.js   # HTML模板
│   ├── styles.js     # CSS样式
│   └── script.js     # JavaScript逻辑
├── wrangler.toml     # Cloudflare 配置文件
├── deploy-cloudflare.sh
├── deploy-github.sh
└── setup-kv.js       # KV 配置脚本
```

## 🔧 技术说明

- **KV命名空间**：使用 `NOTEBOOK_DATA` 存储笔记数据
- **数据格式**：每个笔记包含 id, name, url, description, tags, createdAt
- **分页**：每页最多显示50条笔记
- **详情面板**：右侧滑出式面板显示完整信息
